#!/bin/sh
# Docker Compose 环境变量配置
# 复制此文件为 .env 并修改相应值

# ========== 端口配置 ==========
# 后端 ETM Agent API 端口（默认 8000）
BACKEND_PORT=8000

# DataClean API 端口（默认 8001）
API_PORT=8001

# 前端端口（默认 3000）
FRONTEND_PORT=3000

# ========== CORS 配置 ==========
# CORS 允许的源（多个用逗号分隔）
# 生产环境请设置为实际的前端域名
ALLOWED_ORIGINS=http://localhost:3000,https://yourdomain.com

# ========== 前端环境变量 ==========
# 前端访问后端 ETM Agent API 的地址
# 如使用 Nginx 反代，设置为 https://api.yourdomain.com
# 如直接暴露端口，设置为 http://your-server-ip:8000
NEXT_PUBLIC_API_URL=http://localhost:8000

# 前端访问 DataClean API 的地址
# 如使用 Nginx 反代，设置为 https://dataclean.yourdomain.com
# 如直接暴露端口，设置为 http://your-server-ip:8001
NEXT_PUBLIC_DATACLEAN_API_URL=http://localhost:8001

# ========== 后端配置 ==========
# 千问 API Key（必填，否则 AI 相关功能不可用）
QWEN_API_KEY=your-qwen-api-key

# JWT 密钥（用于用户认证，生产请改为随机字符串）
SECRET_KEY=change-this-to-a-random-secret-key

# 对外域名（用于 CORS，仅本机可填 localhost）
DOMAIN=localhost

# ========== 数据库（Docker Compose 中后端使用 PostgreSQL） ==========
# 与 docker-compose 中 db 服务的 POSTGRES_PASSWORD 保持一致
POSTGRES_PASSWORD=theta123
